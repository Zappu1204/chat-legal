package com.congdinh.vivuchat.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenApiConfig {
    // OpenAPI configuration disabled due to compatibility issues with Spring Boot 3.4.3
    // Will be re-enabled when compatible versions of SpringDoc are available
}
